{
  // Hereda las reglas comunes
  "extends": "../../tsconfig.base.json",

  // Reglas específicas para esta aplicación
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },

  // Archivos a compilar
  "include": ["src"],

  // ¡Importante! Declara las dos dependencias del proyecto
  "references": [
    { "path": "../../packages/shared" },
    { "path": "../../packages/db" }
  ]
}