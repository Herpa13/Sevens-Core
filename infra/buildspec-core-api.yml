version: 0.2

phases:
  install:
    commands:
      - corepack enable
      - corepack prepare yarn@4.9.4 --activate
      - yarn config set nodeLinker node-modules
      - yarn workspaces focus @sevens/core-gateway --all --production
  build:
    commands:
      - cd apps/core-gateway
      - yarn build
      - zip -r ../../core-gateway.zip dist node_modules package.json
artifacts:
  files:
    - core-gateway.zip
